#!/usr/bin/env python
# This file is required for the tox testing framework
# It should reside in the same directory as 'tox.ini'
# To use tox, just run 'tox' from the same directory as this file.

import setuptools

if __name__ == "__main__":
    setuptools.setup()
